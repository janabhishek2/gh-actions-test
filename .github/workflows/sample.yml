name: 'Sample commands'
on: [push, pull_request]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: eccho "Hello World this is sample"
      - name:  multiline-script
        run: |
          node -v
          npm -v
  job2:
    runs-on: ubuntu-latest
    steps:
     - name: Basic commands
       run: |
         cat /etc/*release*
         python -v
         node -v
         npm -v
         pwd


        
